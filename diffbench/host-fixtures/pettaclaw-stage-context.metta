!(import! &self (library pettaclaw-godel lib_mettaclaw))

!(once (initLoop))
!(once (initMemory))
!(once (initChannels))
!(once (receive))
!(once (lastMessageIsHuman))
!(once (maybeHeartbeat))
!(once (getContext))
